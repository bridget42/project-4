# project-4
This User Management Admin Panel allows administrators to perform all CRUD (Create, Read, Update, Delete) operations on user accounts. Users have different roles (Admin, Manager, Attendant) and statuses (Active/Inactive). The application stores all data in the browser's localStorage, ensuring persistence across page reloads.
